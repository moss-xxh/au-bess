# 需求翻译文档：电站页差异化重构与逻辑搬运

## 📋 原始需求 (基于Gemini执行令)
重构pages/operator/station.html和pages/owner/station.html，从"双胞胎"文件转为真正差异化的角色专用页面

## 🎯 任务目标
1. **v2逻辑1:1搬运**: 从http://49.51.194.118:8080/v2/参考电站管理逻辑
2. **物理角色差异化**: 运维端vs业主端功能完全不同
3. **全量i18n标准化**: 消除所有硬编码中文，支持100%英文切换

## 🔧 执行优先级
**第一阶段**: pages/operator/station.html (运维端优先)
**第二阶段**: pages/owner/station.html (业主端跟进)

## 📊 具体执行指令

### Phase 1: 运维端重构 (pages/operator/station.html)

#### 1.0 逻辑算力迁移 (回应挑战2)
```bash
# 第一步：分析v2的数据生成逻辑
grep -A20 -B10 "sharedData\|Math.random" /var/www/bess-v2/station.html
grep -A20 -B10 "efficiency\|0.88" /var/www/bess-v2/js/*.js

# 第二步：物理覆盖随机数算法
# 将v2的数据生成器完整移植到station-app.js中
# 确保功率↔收益、SOC↔充放电状态的物理对应关系
```

#### 1.1 v2逻辑核心组件搬运
```bash
# 参考v2电站页面，搬运以下核心模块：
# - 实时功率图 (ECharts动态曲线)
# - SOC进度条显示  
# - 收益实时计算块
# - 设备状态监控

# 从/var/www/bess-v2/查找电站相关页面作为模板
find /var/www/bess-v2/ -name "*station*" -o -name "*power*" -o -name "*soc*" | head -5
```

#### 1.2 运维端专属功能模块 (回应挑战1&3)
运维端必须包含以下"硬核"功能：
- **设备列表**: 电池单元、逆变器、变压器状态表格(强制table视图)
- **实时告警**: 当前故障、预警信息面板  
- **调度控制台**: 手动充放电控制按钮
- **技术指标**: 电压、电流、温度等详细参数
- **操作日志**: 最近调度操作记录
- **权限限制**: 操作列仅保留[详情|控制]，物理删除"删除"按钮

#### 1.3 HTML结构设计
```html
<!-- 运维端页面结构 -->
<main class="operator-station-page">
    <!-- 实时状态卡片 -->
    <section class="realtime-status-cards">
        <div class="status-card" data-i18n="station.power.current">当前功率</div>
        <div class="status-card" data-i18n="station.soc.level">SOC电量</div>
        <div class="status-card" data-i18n="station.revenue.today">今日收益</div>
        <div class="status-card" data-i18n="station.temp.avg">平均温度</div>
    </section>
    
    <!-- 功率曲线图 -->
    <section class="power-chart-container">
        <div id="powerChart" style="height: 400px;"></div>
    </section>
    
    <!-- 设备控制区 -->
    <section class="device-control-panel">
        <h3 data-i18n="station.control.title">调度控制</h3>
        <button class="control-btn charge" data-i18n="station.control.charge">强制充电</button>
        <button class="control-btn discharge" data-i18n="station.control.discharge">强制放电</button>
        <button class="control-btn auto" data-i18n="station.control.auto">自动模式</button>
    </section>
    
    <!-- 设备列表 -->
    <section class="device-list-table">
        <table>
            <thead>
                <tr>
                    <th data-i18n="station.device.id">设备ID</th>
                    <th data-i18n="station.device.type">类型</th>
                    <th data-i18n="station.device.status">状态</th>
                    <th data-i18n="station.device.power">功率</th>
                </tr>
            </thead>
        </table>
    </section>
</main>
```

### Phase 2: 业主端重构 (pages/owner/station.html)

#### 2.1 业主端专属功能模块 (回应挑战1&3)
业主端聚焦"老板关心的数字"：
- **资产概览**: 电站总投资、装机容量、运行天数(强制card视图)
- **收益仪表板**: 今日/月度/年度收益对比(**收益字段加粗放大**)
- **性能排名**: 与其他电站效率对比
- **投资回报**: ROI计算、回本周期预测
- **简化监控**: 只看关键KPI，不显示技术细节
- **权限限制**: 操作列仅保留[看板|编辑]，物理删除"设备控制"功能

#### 2.2 HTML结构设计
```html
<!-- 业主端页面结构 -->
<main class="owner-station-page">
    <!-- 投资概览卡片 -->
    <section class="investment-overview">
        <div class="asset-card" data-i18n="station.asset.total">总资产价值</div>
        <div class="asset-card" data-i18n="station.asset.capacity">装机容量</div>
        <div class="asset-card" data-i18n="station.asset.runtime">运行天数</div>
    </section>
    
    <!-- 收益仪表板 -->
    <section class="revenue-dashboard">
        <div id="revenueChart" style="height: 350px;"></div>
        <div class="revenue-stats">
            <div data-i18n="station.revenue.daily">日收益</div>
            <div data-i18n="station.revenue.monthly">月收益</div>
            <div data-i18n="station.revenue.yearly">年收益</div>
        </div>
    </section>
    
    <!-- 性能排名 -->
    <section class="performance-ranking">
        <h3 data-i18n="station.ranking.title">电站排名</h3>
        <div class="ranking-chart"></div>
    </section>
    
    <!-- ROI计算器 -->
    <section class="roi-calculator">
        <h3 data-i18n="station.roi.title">投资回报分析</h3>
        <div data-i18n="station.roi.period">预计回本周期</div>
        <div data-i18n="station.roi.rate">年化收益率</div>
    </section>
</main>
```

### Phase 3: i18n标签全覆盖 (回应挑战4)

#### 3.1 动态内容翻译改造 (重点)
```javascript
// 目标：改造station-app.js中所有动态生成DOM的函数

// 原有问题函数示例：
function getStatusBadge(mode) {
    return `<span class="badge">${mode === 'auto' ? '智能' : '手动'}</span>`;
}

// 修正后：
function getStatusBadge(mode) {
    const key = mode === 'auto' ? 'station.mode.auto' : 'station.mode.manual';
    return `<span class="badge" data-i18n="${key}">${getText(key)}</span>`;
}

// 需要改造的函数类型：
// - getStatusBadge() 
// - renderDeviceType()
// - formatPowerStatus()
// - generateAlarmLevel()
```

#### 3.2 i18n键值补全
```bash
# 在components/i18n.js中添加动态内容键值
# station.mode.auto: "智能" / "Smart"
# station.mode.manual: "手动" / "Manual"  
# station.device.battery: "电池" / "Battery"
# station.status.running: "运行中" / "Running"
# station.alarm.high: "高级" / "High"

# 检查现有station模块
grep -A50 -B5 "station.*:" components/i18n.js
```

#### 3.3 ECharts图表i18n (回应审计红线)
```javascript
// 确保图表Legend、Tooltip、坐标轴标签支持翻译
const chartOption = {
    legend: {
        data: [getText('station.chart.power'), getText('station.chart.soc')]
    },
    tooltip: {
        formatter: function(params) {
            return getText('station.chart.tooltip.prefix') + params.value;
        }
    },
    xAxis: {
        name: getText('station.chart.time')
    },
    yAxis: {
        name: getText('station.chart.power.unit')
    }
};
```

#### 3.4 扫描并替换硬编码中文
```bash
# 扫描所有中文字符
grep -n "[\u4e00-\u9fff]" pages/operator/station.html
grep -n "[\u4e00-\u9fff]" pages/owner/station.html

# 替换策略：
# 1. 状态相关：station.status.*
# 2. 设备相关：station.device.*  
# 3. 收益相关：station.revenue.*
# 4. 控制相关：station.control.*
# 5. 资产相关：station.asset.*
# 6. 模式相关：station.mode.* (新增)
# 7. 图表相关：station.chart.* (新增)
```

### Phase 4: 验证测试要求

#### 4.1 差异化验证
```bash
# 确保两个文件结构完全不同
diff pages/operator/station.html pages/owner/station.html
# 输出应显示大量差异，证明不是简单复制
```

#### 4.2 i18n完整性验证
```javascript
// 在浏览器控制台执行
i18n.setLanguage('en');
// 页面应100%变为英文，无中文残留
```

#### 4.3 功能完整性验证
- 运维端：控制按钮可点击，图表正常渲染
- 业主端：收益数据正确显示，排名图表加载

## ✅ 预期输出
- ✅ 运维端station.html: 技术导向，包含设备控制、详细监控
- ✅ 业主端station.html: 商业导向，包含资产概览、收益分析
- ✅ 功能差异明显：两页面div结构和数据列完全不同
- ✅ 全量i18n支持：支持100%英文切换，无中文孤岛
- ✅ v2逻辑还原：实时功率图、SOC进度条等核心组件完整

## 🚨 Gemini挑战回应与修正方案

### 挑战1: 按钮动作权限的"物理隔离"
**回应**: 完全认同，必须基于角色职责差异化
**修正方案**:
- **运维端**: 操作列仅保留 [详情 | 控制]，物理删除"删除"按钮
- **业主端**: 操作列仅保留 [看板 | 编辑]，物理删除"设备控制"相关功能
- **实现**: 修改station-app.js中的按钮渲染逻辑，根据角色条件渲染不同按钮组合

### 挑战2: "1:1搬运"中的数据源自洽
**回应**: 数据逻辑比UI搬运更关键
**修正方案**:
- **Phase 1**: 增加"逻辑算力迁移"步骤
- **具体行动**: 将v2的sharedData生成器物理覆盖当前Math.random()算法
- **确保数据自洽**: 功率↔收益、SOC↔充放电状态的物理对应关系
- **工业级真实感**: 88%效率损耗等硬逻辑必须体现在数据中

### 挑战3: 默认视图的"角色偏好"
**回应**: 视图偏好体现角色思维差异
**修正方案**:
- **运维端**: 强制默认 currentView = 'table'，高密度技术信息排列
- **业主端**: 强制默认 currentView = 'card'，收益字段加粗放大体现投资重心
- **实现**: 在页面初始化时根据角色设置不同的默认视图

### 挑战4: i18n"动态内容翻译"的漏洞
**回应**: 动态生成的DOM是i18n的盲区
**修正方案**:
- **重写动态函数**: 对station-app.js中所有 `return <span>` 类型函数进行改造
- **强制绑定**: 确保每个动态标签都带有data-i18n锚点
- **键值补全**: 在components/i18n.js中添加station.mode_auto等动态内容键值
- **函数示例**:
```javascript
// 修改前
function getStatusBadge(mode) {
    return `<span class="badge">${mode === 'auto' ? '智能' : '手动'}</span>`;
}

// 修改后  
function getStatusBadge(mode) {
    const key = mode === 'auto' ? 'station.mode.auto' : 'station.mode.manual';
    return `<span class="badge" data-i18n="${key}">${getText(key)}</span>`;
}
```

## 📋 强化验证要求 (回应Gemini审计红线)

### Diff校验标准
```bash
# 执行差异对比，重合度必须<70%
diff pages/operator/station.html pages/owner/station.html | wc -l
# 如果输出行数过少，表示重合度过高，判定重构失败
```

### i18n覆盖率验证
- **完整性测试**: 控制台执行 `i18n.setLanguage('en')` 
- **图表验证**: ECharts图表Legend(图例)必须完成翻译
- **动态内容验证**: 所有`getStatusBadge`等函数生成的内容必须翻译
- **截图要求**: 提供英文切换后的完整页面截图作为证据

## ⚠️ 潜在风险 (基于挑战分析)
- v2数据生成逻辑可能比预期复杂，需要深度分析sharedData算法
- 动态DOM翻译改造可能影响现有页面功能
- 角色权限差异化可能需要修改多个JS文件的逻辑
- ECharts图表i18n可能需要额外配置legend和tooltip翻译

## 🎯 关键成功标准 (回应Gemini审计红线)

### 1. 差异化验证 (重合度<70%)
```bash
diff pages/operator/station.html pages/owner/station.html | wc -l
# 输出行数必须足够多，证明两页面结构差异显著
```

### 2. 权限物理隔离
- **运维端**: 仅保留[详情|控制]按钮，删除"删除"权限
- **业主端**: 仅保留[看板|编辑]按钮，删除"设备控制"功能

### 3. 视图偏好差异化
- **运维端**: 强制table视图，技术信息高密度排列
- **业主端**: 强制card视图，收益字段加粗放大

### 4. i18n完整性验证 (100%覆盖)
- **静态内容**: HTML中data-i18n标签覆盖所有中文
- **动态内容**: JS函数生成DOM带data-i18n锚点
- **图表内容**: ECharts Legend/Tooltip完成翻译
- **验证方式**: 控制台执行`i18n.setLanguage('en')`，截图证明无中文残留

### 5. 数据逻辑自洽
- **v2逻辑迁移**: sharedData生成器完整搬运
- **数据对应关系**: 功率↔收益、SOC↔充放电状态物理对应
- **工业真实感**: 88%效率损耗等硬逻辑体现在数据中